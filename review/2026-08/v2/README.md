# Review render v2 — refinement pass

Temporary, NOT approved, NOT production. Second iteration after the
first visual review, per Phase 7C composition refinement feedback:

- Month title (Hero Artwork): resized from a 720px-max-width treatment
  to a fixed 140px height (requested range: 120-160px).
- Weekday identity artwork: resized from 110px to 100px height
  (requested range: 90-110px).
- Weekday identity artwork now replaces the plain SUN/MON/etc. label
  entirely for any weekday it applies to, instead of sitting alongside
  it — the artwork is the complete weekday label.
- Canvas: unchanged, 1800x1200 (measured height came back to exactly
  the original dimensions -- no growth was needed with the reduced
  artwork sizes).

See `../` (v1) for the prior iteration and `../../..` (repo root) for
the actual, unaffected production preview.
